# 自動テスト

拡張機能のフォルダを開き、次を実行します。

```powershell
powershell -ExecutionPolicy Bypass -File .\run-tests.ps1
```

Node.js が通常のコマンドとして登録されていない場合は、Codexに同梱された
Node.jsも自動的に検索します。

現在のテストケースでは、以下を確認します。

- 「ソース」という語を含むツールチップを追加ダイアログと誤認しないこと
- 操作要素を持つ本物のソース追加ダイアログを認識すること
- YouTube SPA遷移でURLと動画情報のIDが一致するまで待つこと
- タイトル・サムネイルA/Bテスト時にoEmbedの標準タイトルをソース照合へ使用すること
- URL確定Enterと「挿入」クリックを二重送信しないこと
- 仮想スクロールされた画面外の既存ソースを探索すること
- `aria-label` のない正式ソース行を認識すること
- 同じ動画が複数登録済みでも1件だけを選択すること
- 「すべて選択」による全ソースの一括解除
- 対象動画のソース1件だけの選択
- 対象1件が既に選択済みの場合にチェックを変更しないこと
- 一括解除後に対象1件だけ残った場合、その状態を正常として使用すること
- 一括解除の画面反映が遅れても、クリック前の古い全選択状態を完了と誤判定しないこと
- 新規ソース追加後の再描画でチェックが外れた場合の再選択
- URLだけの一時行ではなく、処理済みの正式ソース行になるまで待つこと
- 作成前後のUUID一覧から新規マインドマップだけを判定すること
- 生成中カードを完成扱いしないこと
- 同じUUIDの完成カードだけを完成扱いすること
- `flowchart` アイコンや特定メインボタンがない現行画面の完成カードを認識すること
- 既存カードが同名でも新規UUIDの改名完了と誤判定しないこと
- 完成候補が子要素でも親カードからUUIDを取得すること
- DOM要素が再利用されても新規UUIDなら新規マップと判定すること
- 日本語動画へ日本語マインドマップ作成指示を設定すること
- 作成ボタンだけではノートブック一覧の読み込み完了と判定しないこと
- 画面遷移後に作成途中の無題ノートブックを引き継ぐこと
- 最新版のGemini Notebook操作スクリプトでは無意味な再読み込みをしないこと
- タブの読み込み完了を状態確認すること
- ノートブック画面遷移で通信が切れても新ページで処理を継続すること
- DOM変更通知で操作完了を即時判定し、固定sleepや固定間隔ポーリングを使わないこと
- タブ更新イベントで読み込み完了を検出し、固定間隔ポーリングを使わないこと
- 正常完了時に監視処理と上限タイマーを解除すること
- 拡張機能名、Gemini Notebook URL、ログファイル名
- ソース処理と全体処理の待機上限

テストは実際のGemini Notebookで確認したDOM構造を模したフィクスチャを使用します。

全受け入れ条件、追加すべき自動テスト、実画面E2Eテストは
`TEST_SPECIFICATION.md` を参照してください。
