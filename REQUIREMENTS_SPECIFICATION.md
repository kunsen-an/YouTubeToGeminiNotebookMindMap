# YouTube to Gemini Notebook Mind Map 要求仕様書

## 1. 文書の位置付け

本書は、既存コードを参照せず別の開発者またはAIモデルが、現在と同等のChrome拡張機能を再実装するための正本である。

機能名、判定条件、安全条件、待機条件を省略してはならない。特にソース選択、マインドマップ完成判定、既存マインドマップ保護、画面遷移後の処理再開は必須要件である。

テスト要件は `TEST_SPECIFICATION.md` に定める。各要件の識別番号はテスト仕様との対応付けに使用する。

## 2. 製品識別情報

| 項目 | 要求値 |
|---|---|
| 拡張機能名 | `YouTube to Gemini Notebook Mind Map` |
| 配布フォルダー名 | `YouTubeToGeminiNotebookMindMap` |
| Manifest | Chrome Manifest V3 |
| Gemini Notebook URL | `https://notebook.google.com/` |
| 操作方式 | Gemini NotebookのWeb UIをContent Scriptで操作 |
| デバッグファイル名 | `youtube-to-gemini-notebook-debug-<ISO日時>.json` |
| 主言語 | 日本語UI。英語UIの主要文言にも対応 |

REQ-ID-001: 拡張機能名、ポップアップ、進捗、エラー、デバッグファイル名には旧製品名 `NotebookLM` を表示しないこと。

REQ-ID-002: デバッグファイル名には `notebooklm` を含めないこと。

REQ-ID-003: 16、32、48、128ピクセルの専用アイコンをManifestとアクションへ設定すること。

## 3. 目的と非目的

REQ-GOAL-001: 字幕のあるYouTube動画を、YouTubeチャンネル名と同名のGemini Notebookノートブックへソースとして登録すること。

REQ-GOAL-002: 登録対象の動画ソース1件だけを選択し、Studioのビジュアルなマインドマップを生成すること。

REQ-GOAL-003: 作成した新規マインドマップの名前を、生成完了後にYouTube動画タイトルへ変更すること。

REQ-GOAL-004: 日本語動画のマインドマップは日本語で生成すること。

次は非目的とする。

- Gemini Notebook API、NotebookLM API、Enterprise API、非公開APIの利用
- チャット欄へMarkdownマインドマップを生成する方式
- 複数のソースをまとめたマインドマップの生成
- 字幕のない動画の登録

## 4. 構成と責務

REQ-ARC-001: 最低限、次の構成へ分離すること。

| 構成 | 責務 |
|---|---|
| `manifest.json` | 権限、Content Script、アイコン、ポップアップ、Service Worker |
| `content/youtube.js` | YouTube動画情報と字幕・言語情報の取得 |
| `popup/` | 動画情報、実行、進捗、エラー、デバッグ保存 |
| `background.js` | タブ制御、バージョン確認、画面遷移をまたぐ処理再開、ログ保存 |
| `content/notebooklm.js` | Gemini Notebook Web UIの自動操作 |
| `content/pageLogger.js` | DevToolsに依存しない画面状態ログ補助 |
| `tests/` | DOMフィクスチャを使う自動回帰テスト |

REQ-ARC-002: Content ScriptとService Worker間の要求には、動画タイトル、チャンネル名、正規化URL、サムネイル、字幕有無、動画言語コードを含めること。

REQ-ARC-003: Gemini Notebook操作スクリプトとService Workerは同一のバージョン識別子を持ち、不一致を検出できること。

REQ-ARC-004: 機能変更と不具合修正は、実装、自動テスト、`REQUIREMENTS_SPECIFICATION.md`、`TEST_SPECIFICATION.md`へ同時に反映すること。`REBUILD_REQUIREMENTS.md`は正本への索引とし、別系統の古い要求を保持しないこと。

## 5. 必要権限

REQ-SEC-001: 権限は次を上限として必要最小限にすること。

- `activeTab`
- `scripting`
- `tabs`
- `windows`
- `storage`
- `downloads`

REQ-SEC-002: Host Permissionは次を対象とすること。

- `https://www.youtube.com/*`
- `https://youtube.com/*`
- `https://notebook.google.com/*`

旧URLからの移行互換が必要な場合のみ `https://notebooklm.google.com/*` を残してよい。

REQ-SEC-003: YouTubeまたはGemini Notebook以外へ動画情報やデバッグ情報を送信しないこと。

## 6. YouTube動画情報の取得

REQ-YT-001: YouTubeの通常動画ページ `/watch?v=...` だけを処理対象とすること。

REQ-YT-002: 次を取得すること。

- 動画タイトル
- YouTube公開メタデータ上のソース照合用タイトル
- チャンネル名
- Canonical YouTube URL
- 動画ID
- サムネイルURL
- 字幕の有無
- 音声または字幕の言語コード

REQ-YT-003: 動画タイトルとチャンネル名は、安定したDOM、構造化情報、Open Graph情報の順に複数候補から取得できること。

REQ-YT-004: 字幕情報は `ytInitialPlayerResponse` の字幕トラックを優先し、取得できない場合は「文字起こし」「字幕」「Transcript」「Captions」操作の存在を補助判定に用いること。

REQ-YT-005: 音声言語は `defaultAudioLanguage` を優先し、取得できない場合は既定字幕トラックの `languageCode` を使用すること。

REQ-YT-006: 字幕がない場合は「字幕がないため追加できません」と表示し、Gemini Notebookを開かないこと。

REQ-YT-007: YouTubeのSPA遷移では、現在URLの動画IDと、描画中ページまたは `ytInitialPlayerResponse` の動画IDが一致するまで動画情報を確定しないこと。別動画のタイトル・字幕状態を混在させてはならない。

REQ-YT-008: YouTubeのタイトルまたはタイトル・サムネイルA/Bテストにより、ブラウザ表示タイトルとGemini Notebookが採用するタイトルが異なる場合に備え、YouTube oEmbedの公開メタデータからソース照合用タイトルを取得すること。ポップアップ表示とマインドマップ改名にはブラウザ表示タイトルを使用し、既存ソースの照合にはソース照合用タイトルを使用すること。

## 7. ポップアップUI

REQ-POP-001: YouTube動画ページでは、サムネイル、動画タイトル、チャンネル名、「Gemini Notebookに追加」ボタンを表示すること。

REQ-POP-002: YouTube動画ページ以外では、「動画ページで開いてください」を表示すること。ただし、Gemini Notebook上で開いた場合は動画ページエラーではなく現在の進捗表示を表示すること。

REQ-POP-003: 読み込み表示、非対象ページ表示、動画パネルを同時表示しないこと。`hidden` 要素はCSSでも確実に非表示にすること。

REQ-POP-004: 実行中は追加ボタンを無効化し、二重実行を防止すること。

REQ-POP-005: 完了またはエラーまで進捗を表示し、エラー時は再実行可能な状態へ戻すこと。

REQ-POP-006: 正常終了済みのログは、次回ポップアップ利用時に処理パネルへ再表示しないこと。

REQ-POP-007: 拡張機能のインストール・再読み込み時、および新規処理開始時に古い進捗ログと詳細ログを初期化すること。

## 8. Gemini Notebookタブの準備

REQ-TAB-001: 既存の `notebook.google.com` タブがあれば再利用し、なければ新規作成すること。

REQ-TAB-002: 対象タブとウィンドウを前面にすること。

REQ-TAB-003: 操作スクリプトのバージョンが最新版なら、無意味なタブ再読み込みを行わないこと。

REQ-TAB-004: バージョンが古い、または操作スクリプトがない場合のみ再読み込みまたは再注入を行うこと。

REQ-TAB-005: タブ読み込み完了は `chrome.tabs.onUpdated` の状態変更イベントで検出し、30秒以内に完了しなければ明示的に失敗すること。固定間隔で `chrome.tabs.get` を繰り返してはならないこと。

REQ-TAB-006: 別ノートブックからホームへ戻る、ホームから既存ノートブックを開く、新規ノートブックを開く、というページ遷移でMessage Channelが閉じても処理を終了しないこと。

REQ-TAB-007: ページ遷移を検出した場合は新ページの読み込み完了、操作スクリプトのバージョン、メッセージ応答を再確認し、同じ動画処理を再開すること。再接続試行上限は5回とすること。

REQ-TAB-008: 通常の操作エラーを画面遷移エラーとして再試行してはならないこと。

## 9. ノートブックの選択と作成

REQ-NB-001: ノートブック名はYouTubeチャンネル名とすること。

REQ-NB-002: 現在開いているノートブックが対象チャンネルと一致する場合は、そのまま使用すること。

REQ-NB-003: 別のノートブックが開いている場合はホームへ戻り、一覧から対象チャンネルのノートブックを探すこと。

REQ-NB-004: 既存ノートブックがあれば重複作成せず、そのノートブックを開くこと。

REQ-NB-005: 候補カード内のリンクを優先して開き、現在URLとノートブック画面の表示を確認すること。

REQ-NB-006: 既存ノートブックがない場合のみ新規作成し、タイトル入力欄をYouTubeチャンネル名へ変更すること。

REQ-NB-007: ノートブックを開いたことはURLだけでなく、ソース追加操作またはノートブック固有UIの表示でも確認すること。

REQ-NB-008: ホーム画面の一覧読み込み完了は、既存ノートブックカードが表示された状態、または一覧コンテナに明示的な空状態が表示された状態で判定すること。「ノートブックを作成」ボタンが表示されただけで一覧を空と判定してはならない。

REQ-NB-009: 新規作成ボタンを押す前に、作成対象のチャンネル名を一時状態として保存すること。ページ遷移後に無題ノートブックが開いた場合は、その一時状態と一致する作成途中のノートブックとして引き継ぎ、チャンネル名を設定すること。

REQ-NB-010: 画面遷移で処理を再開した際、作成途中の無題ノートブックを放置して追加の新規ノートブックを作成してはならないこと。一時状態は命名完了、既存ノートブック利用、または新規処理開始時に消去すること。

## 10. YouTubeソースの追加と重複防止

REQ-SRC-001: 追加前に対象動画IDまたは十分に長い動画タイトルで既存ソースを確認すること。

REQ-SRC-002: 同じ動画が既に存在する場合は新規追加せず、既存の該当ソースを選択すること。

REQ-SRC-003: 既存判定に失敗して同じ動画を複数回登録してはならないこと。

REQ-SRC-004: 新規追加時は「ソースを追加」からYouTubeまたはウェブサイトURL追加画面を開くこと。

REQ-SRC-005: URL入力欄とソース検索欄を区別し、検索欄へURLを入力しないこと。

REQ-SRC-006: Angularフォームにはネイティブsetter、フォーカス、`beforeinput`、`input`、`change` 等を使用し、画面側のフォーム状態を更新すること。

REQ-SRC-007: URL確定後、URL入力画面が閉じたこと、ソース件数の増加または対象動画の表示、処理中表示の消滅を確認すること。

REQ-SRC-008: ソース追加ダイアログが重なった場合は最前面を操作し、処理後は最大5回の確認で不要なダイアログをすべて閉じること。

REQ-SRC-009: ソース一覧の空状態説明文を、追加済みソースとして判定しないこと。

REQ-SRC-010: ノートブックを開いた直後に表示されるツールチップや説明ポップアップをソース追加ダイアログと判定しないこと。ソース追加ダイアログは、ソース関連の文言に加えて、入力欄、ボタン、選択カード等の操作要素を持つことを確認すること。

REQ-SRC-011: URL入力確定のEnterと「挿入」ボタンのクリックは、それぞれ1回だけ送信すること。同じ操作へ合成クリックイベントと `click()` を重ねて二重送信してはならない。

REQ-SRC-012: ソースが多数あり一覧が仮想スクロールされる場合は、現在描画中の行だけで既存判定を終えず、スクロール可能な一覧全体を探索すること。

## 11. 対象ソース1件だけの選択

REQ-SEL-001: マインドマップ作成前に、対象動画のソースを動画ID、YouTube公開メタデータ上のソース照合用タイトル、または動画タイトルで特定すること。

REQ-SEL-002: 対象を0件しか特定できない場合は、すべてのソースを使わず安全に停止すること。同じ動画が過去の不具合等で複数登録されている場合は、そのうち1件だけを選び、他の重複ソースを選択しないこと。

REQ-SEL-003: 新規ソースはURLだけの一時行ではなく、動画タイトルへ解決され、処理中表示がなく、チェックと操作メニューが有効な正式ソース行になるまで待つこと。待機上限は90秒とすること。

REQ-SEL-004: 既に対象1件だけが選択されている場合は、そのチェックを操作しないこと。

REQ-SEL-005: 複数選択または別ソース選択の場合は、「すべて選択」を使用して一括解除すること。ソースを1件ずつ解除してはならないこと。

REQ-SEL-006: 部分選択状態で「すべて選択」が一括解除にならないUIの場合は、一度全選択状態へ揃えた後、もう一度「すべて選択」を押して一括解除すること。

REQ-SEL-007: 一括解除後に対象ソースだけを選択すること。Angularの再描画でチェックが外れた場合は最大3回再試行すること。

REQ-SEL-008: 現在のDOMを再取得し、選択数が1件で、その1件が対象動画であることを確認してから次へ進むこと。

REQ-SEL-009: 選択確定後に対象チェックを再度外してはならないこと。

REQ-SEL-010: 最終確認で選択数が1件でない、または選択内容が対象動画でない場合はマインドマップ作成を中止すること。

REQ-SEL-011: 「すべて選択」を切り替えた結果、全件0件ではなく対象動画1件だけが選択された状態になった場合も正常な一括解除結果として受け入れ、対象チェックを再操作せず次へ進むこと。

REQ-SEL-012: 「すべて選択」をクリックした後は、クリック前と異なる選択状態がDOMへ反映されたことを確認してから結果を判定すること。クリック直後に残っている古い全選択状態を操作完了と判定してはならないこと。

REQ-SEL-013: 正式ソース行のチェックボックスに `aria-label` がない場合も、行タイトル、チェック状態、操作メニュー等から操作可能なソースとして認識すること。

## 12. 日本語マインドマップ指定

REQ-LANG-001: 動画言語コードが `ja` または `jpn` で始まる場合は日本語動画と判定すること。

REQ-LANG-002: 言語コードを取得できない、または不正確な場合でも、動画タイトルにひらがな・カタカナが含まれれば日本語動画と判定すること。

REQ-LANG-003: 日本語動画では、マインドマップ作成ダイアログの「希望するトピック」欄へ次を入力すること。

`動画の内容を日本語で整理し、マインドマップ全体を日本語で作成してください。`

REQ-LANG-004: 指示文が入力欄へ反映されたことを確認してから生成ボタンを押すこと。

REQ-LANG-005: 日本語ではない動画へ日本語指定を追加しないこと。

## 13. マインドマップの生成

REQ-MM-001: Studioのビジュアルな「マインドマップ」を使用すること。チャット生成で代用しないこと。

REQ-MM-002: 作成ボタンを押す直前に、既存マインドマップ一覧を取得すること。

REQ-MM-003: 一覧項目の安定識別子は `.artifact-labels[id]` の `artifact-labels-<UUID>` を第一候補とすること。

REQ-MM-004: 生成開始後、作成前一覧に含まれないUUIDを持つ新規項目だけを候補にすること。

REQ-MM-005: UUIDが取得できない場合に限り、タイトルキーと件数差分を安全な補助判定として使用してよい。候補が一意でなければ停止すること。

REQ-MM-006: 次をすべて満たすまで生成完了と判定しないこと。

- 「生成中」「処理中」「loading」等がない
- shimmer、spinner、回転アイコン、`aria-busy` がない
- `.artifact-labels[id]` が存在する
- タイトルが空でない
- 操作メニューボタンが存在し有効

`flowchart` アイコン文字列および特定のメインボタンは画面変更で存在しない場合があるため、必須の完成条件にしてはならない。

REQ-MM-007: REQ-MM-006の完成状態がすべて揃った後にのみ改名へ進むこと。生成開始からの経過時間を完成条件に使用してはならない。

REQ-MM-008: マインドマップ生成完了の待機上限は240秒とすること。

REQ-MM-009: 作成後一覧の総件数が作成前より必ず1件だけ増えることを完成判定の前提にしてはならない。作成前に存在しなかったUUIDが一意に特定できることを優先すること。

## 14. 新規マインドマップの改名と既存項目保護

REQ-REN-001: マインドマップ名の変更は、生成完了を確認した後に行うこと。

REQ-REN-002: 作成前のUUID一覧に含まれず、作成後に出現したUUIDの項目だけを改名すること。

REQ-REN-003: DOM要素の同一性を新旧判定に使用しないこと。Angularは既存DOM要素を新しいカードへ再利用する場合がある。

REQ-REN-004: 生成完了時に取得したDOM参照を使い続けず、操作の各段階でUUIDから現在のカードを再取得すること。

REQ-REN-005: 完成候補がカード内部の子要素であっても、最も近い `artifact-library-item` からUUIDと操作メニューを取得すること。

REQ-REN-006: 新規UUIDカード内の操作メニューから「名前を変更」を選び、入力欄へ動画タイトルを設定すること。

REQ-REN-007: Enterまたは保存ボタンで確定し、同じUUIDの `.artifact-title` が動画タイトルと完全一致したことを確認すること。

REQ-REN-008: 別の既存マインドマップが動画タイトルと同名でも、改名成功と判定しないこと。

REQ-REN-009: 新規マインドマップ以外、既存マインドマップ、ソース項目の名前を変更してはならないこと。

## 15. デバッグと監査情報

REQ-LOG-001: 進捗ログと詳細ログを `chrome.storage.local` に保存し、DevToolsを開かなくてもJSONとして書き出せること。

REQ-LOG-002: 保存時は `saveAs: true` とし、ユーザーが保存先を選択できること。

REQ-LOG-003: JSONには少なくとも次を含めること。

- 書き出し日時
- 拡張機能名
- 対象YouTube URL
- 動画タイトル
- チャンネル名
- 動画URL
- 動画言語コード
- 進捗ログ
- 詳細ログ

REQ-LOG-004: 詳細ログには、現在URL、主要ボタン・入力欄・見出し、クリック要素、入力要素、ソース候補、選択状態、作成前後のマインドマップUUID、改名安全判定を記録できること。

REQ-LOG-005: エラーは処理段階を特定できる日本語で表示し、可能な場合は現在URL、対象ノートブック名、対象動画URLを含めること。

REQ-LOG-006: 正常終了時は次回利用時に古いログやエラーをポップアップ上部へ表示しないこと。

## 16. 状態変更待ちと異常終了上限

以下の時間値はすべて異常終了の上限であり、正常処理を遅延させる待機時間ではない。

| 対象 | 異常終了上限または回数 |
|---|---:|
| タブ読み込み | 30秒 |
| 画面遷移再接続 | 最大5回 |
| Gemini Notebook全体処理 | 300秒 |
| ソース追加ボタン | 45秒 |
| URL種別・URL入力・確定 | 各30秒 |
| ソース一覧への追加確認 | 60秒 |
| 新規ソース選択可能化 | 上限90秒。正式ソース行の状態で判定 |
| 「すべて選択」状態変更 | 10秒 |
| 対象ソース選択 | 最大3回。選択数と対象一致で判定 |
| マインドマップ生成開始 | 15秒 |
| マインドマップ生成完了 | 240秒 |
| マインドマップ完成候補 | 完成を示すDOM状態で判定 |
| 改名メニュー・入力・保存 | 原則各15秒 |

REQ-TIME-001: 操作完了を固定時間の経過で判定してはならない。DOM・ARIA・入力値・URL・タブ状態等の完了状態そのものを判定すること。

REQ-TIME-002: 上限到達時は処理を止め、対象外のソースや既存マインドマップを操作して先へ進まないこと。

REQ-TIME-003: 時間は異常終了の上限タイムアウトにだけ使用してよい。正常完了を遅らせる固定sleep、再試行前delay、安定秒数、最低待機秒数を設けないこと。

REQ-TIME-004: DOM状態の再判定は `MutationObserver` と `input`、`change`、`click`、画面遷移等の状態変更イベントで直ちに起動すること。`setInterval` または `setTimeout` の反復による固定間隔ポーリングを使用してはならないこと。

REQ-TIME-005: タブ読み込み状態の再判定は `chrome.tabs.onUpdated` で起動すること。完了または失敗時はイベントリスナーと上限タイマーを解除し、完了後までタイマーを残さないこと。

## 17. DOM操作の堅牢性

REQ-DOM-001: 固定のAngular生成クラス番号、表示順、座標だけに依存しないこと。

REQ-DOM-002: 安定したID、`data-*`、ARIAラベル、役割、日英の表示文言、親子スコープを組み合わせること。

REQ-DOM-003: 同名ボタンが複数ある場合は、ソースダイアログ、Studio、対象マインドマップカード等へスコープして一意にすること。

REQ-DOM-004: 非表示、無効、画面外サイズ0の要素を操作対象にしないこと。

REQ-DOM-005: クリック後は期待状態を確認し、同じ対象を無制限に再クリックしないこと。

REQ-DOM-006: SPAによるDOM交換中の一時的な取得失敗は、次のDOM変更通知で再評価すること。一定時間待ってから推測で処理を続行してはならないこと。

## 18. 受け入れ条件

次のすべてを満たした場合のみ完成とする。

1. 字幕あり動画で動画情報が正しく表示される。
2. 字幕なし動画ではGemini Notebookを操作しない。
3. 別ノートブックが開いていてもホームへ戻り、対象ノートブックを開いて処理を継続する。
4. 対象ノートブックがなければ1つだけ新規作成し、チャンネル名を設定する。
5. 同じ動画を重複ソース登録しない。
6. 新規追加・既存ソースのどちらでも対象ソース1件だけを選択する。
7. ソース数が多い場合も「すべて選択」で一括解除する。
8. 日本語動画では日本語作成指示を入力する。
9. 作成前に存在しなかったUUIDの新規マインドマップだけを完成後に改名する。
10. 既存マインドマップとソース名を一切変更しない。
11. マインドマップ名が動画タイトルと一致する。
12. 正常終了後、次回ポップアップに古いエラーやログを表示しない。
13. デバッグJSONを任意の保存先へ保存できる。
14. `TEST_SPECIFICATION.md` の必須テストに合格する。
15. 正常系の待機はイベント駆動であり、固定sleep・固定間隔ポーリング・安定待機を使用しない。

## 19. 禁止する回帰

- 動画ページであるのに「動画ページで開いてください」を表示する
- 一覧カードの読み込み前に既存ノートブックなしと判断する
- 無題ノートブックを放置して追加のノートブックを作成する
- 「ソース」という語を含むツールチップをソース追加ダイアログと誤認する
- 既存の同名ノートブックがあるのに重複作成する
- 同じ動画ソースを複数登録する
- すべてのソースを選択したままマインドマップを作る
- 対象ソースを選択した直後にチェックを外す
- 「すべて選択」後に対象1件だけが残っているのに解除失敗と判定する
- ソースを1件ずつ解除する
- 「希望するトピック」の入力待ちで停止する
- 生成完了前に既存マインドマップを改名する
- 新規マインドマップではなく既存マインドマップを改名する
- マインドマップではなくソース名を変更する
- DOM要素が再利用されたことを理由に新規UUIDを既存扱いする
- Gemini Notebookのページ遷移で処理を終了する
- 毎回無条件にGemini Notebookタブを再読み込みする
- 操作完了後も固定秒数または「安定待機」の経過を待つ
- 固定間隔のタイマーでDOMまたはタブ状態をポーリングする
- 再試行の前に固定時間のdelayを入れる
- 「すべて選択」クリック直後の古い選択状態を操作結果として判定する
- 正常終了後の古いログやエラーを次回ポップアップへ表示する
