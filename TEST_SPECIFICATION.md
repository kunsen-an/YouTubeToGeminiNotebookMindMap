# YouTube to Gemini Notebook Mind Map テスト仕様書

## 1. 目的

本書は `REQUIREMENTS_SPECIFICATION.md` の実装が正しく、過去に発生した不具合が再発しないことを確認するためのテスト正本である。

DOMフィクスチャによる自動テストだけではGemini Notebookの現在の画面構造を保証できないため、次の3層をすべて実施する。

1. 静的検査
2. DOMフィクスチャを使う自動回帰テスト
3. ログイン済みブラウザを使う実画面E2Eテスト

## 2. 合格基準

TEST-GATE-001: 必須自動テストがすべて成功すること。

TEST-GATE-002: 作業フォルダーと配布フォルダーの双方で同じテスト結果になること。

TEST-GATE-003: 変更したJavaScript全ファイルの構文検査が成功すること。

TEST-GATE-004: 必須E2Eシナリオがすべて成功し、既存ソース名・既存マインドマップ名に変更がないこと。

TEST-GATE-005: 失敗時にデバッグJSONから停止段階、対象動画、対象ノートブック、ソース選択状態、マインドマップUUIDを確認できること。

重大度CriticalまたはHighのケースが1件でも失敗した場合はリリース不可とする。

## 3. テスト環境

### 3.1 自動テスト

- Windows PowerShell
- Node.js `node:test`
- JavaScript VM
- 実画面で確認したGemini Notebook DOMを模したフィクスチャ

実行コマンド:

```powershell
powershell -ExecutionPolicy Bypass -File .\run-tests.ps1
```

### 3.2 E2Eテスト

- 最新安定版Google Chrome
- ログイン済みGoogleアカウント
- Gemini Notebookを利用可能なアカウント
- パッケージ化されていない拡張機能として配布フォルダーを読み込む
- 字幕あり日本語動画、字幕あり英語動画、字幕なし動画
- ソース0件のノートブック
- ソースが多数あるノートブック
- 既存マインドマップが複数あるノートブック

## 4. テストデータ

| データID | 内容 |
|---|---|
| DATA-JA-NEW | 字幕あり、日本語タイトル、新規ソース |
| DATA-JA-EXISTING | 字幕あり、既に同じ動画ソースが1件存在 |
| DATA-EN | 字幕あり、英語音声・英語タイトル |
| DATA-NOCAP | 字幕なし |
| DATA-MANY | 20件以上のソースを持つノートブック |
| DATA-MAPS | 名前が異なる既存マインドマップを3件以上持つノートブック |
| DATA-SAME-TITLE | 既存マインドマップの1件が新規動画タイトルと同名 |
| DATA-OTHER-NB | 処理開始時に別チャンネルのノートブックを開いている状態 |

動画URL、動画ID、動画タイトル、チャンネル名、処理前ソース数、処理前マインドマップUUIDとタイトルを試験記録へ保存すること。

## 5. 静的検査

### TS-STATIC-001 製品識別情報

- 重要度: High
- 対応要件: REQ-ID-001〜003

確認:

- Manifest名が `YouTube to Gemini Notebook Mind Map`
- 配布フォルダー名が `YouTubeToGeminiNotebookMindMap`
- `notebook.google.com` がHost PermissionとContent Script対象
- ログファイル名が `youtube-to-gemini-notebook-debug-`
- ログファイル名に `notebooklm` がない
- 必要な4サイズのアイコンが存在

### TS-STATIC-002 バージョン同期

- 重要度: High
- 対応要件: REQ-ARC-003

`background.js` とGemini Notebook Content Scriptのバージョン識別子が完全一致すること。

### TS-STATIC-003 権限

- 重要度: Medium
- 対応要件: REQ-SEC-001〜003

Manifestに不要な外部ドメイン、広すぎる権限、リモートコードがないこと。

### TS-STATIC-004 待機上限

- 重要度: High
- 対応要件: REQ-TAB-005、REQ-SEL-003、REQ-MM-008、REQ-TIME-001

コード上の上限が次と一致すること。

- タブ30秒
- ソース選択可能化90秒
- 全体300秒
- マインドマップ完成240秒
- 画面遷移再接続5回

### TS-STATIC-005 仕様書の一貫性

- 重要度: High
- 対応要件: REQ-ARC-004

確認:

- 要求仕様に最新のソースダイアログ、ソース選択状態変化、イベント駆動待機の要件が存在する。
- テスト仕様に各要件へ対応する回帰テストが存在する。
- `REBUILD_REQUIREMENTS.md` は正本への索引であり、旧URLや旧製品向けの別要求を含まない。
- 実装とテストのバージョン識別子が一致する。

## 6. YouTube情報取得テスト

### TS-YT-001 通常動画情報

- 重要度: High
- 対応要件: REQ-YT-001〜005

手順:

1. 字幕あり通常動画ページを開く。
2. Content Scriptへ動画情報を要求する。

期待:

- タイトル、チャンネル名、Canonical URL、サムネイルが空でない。
- URLの動画IDが表示中動画と一致する。
- `hasCaptions=true`。
- 取得可能な場合は言語コードが返る。

### TS-YT-002 字幕なし

- 重要度: Critical
- 対応要件: REQ-YT-006

期待:

- 「字幕がないため追加できません」と表示。
- 追加ボタンが無効。
- Gemini Notebookタブを作成・前面化しない。

### TS-YT-003 SPAで別動画へ移動

- 重要度: High

YouTubeのSPA遷移で別動画へ移動後、現在URLの動画IDと描画中ページまたはPlayer Responseの動画IDが一致するまで待ち、古いタイトル・URL・字幕状態を使用しないこと。

### TS-YT-004 タイトル・サムネイルA/Bテスト

- 重要度: Critical
- 対応要件: REQ-YT-008、REQ-SRC-001〜003

同じ動画IDについて、ブラウザ表示タイトルとYouTube oEmbedの標準タイトルが異なるフィクスチャを使用する。

期待:

- ポップアップとマインドマップ名にはブラウザ表示タイトルを使用する。
- Gemini Notebookのソース照合にはoEmbedの標準タイトルを使用する。
- タイトル差を別動画と誤認して同じURLを重複追加しない。

## 7. ポップアップテスト

### TS-POP-001 YouTube動画ページ

期待:

- 読み込み終了後、動画パネルだけが表示される。
- 誤った「動画ページで開いてください」が上部に表示されない。

### TS-POP-002 非動画ページ

期待:

- 「動画ページで開いてください」を表示。
- 動画サムネイルや追加ボタンを表示しない。

### TS-POP-003 Gemini Notebook上で再度開く

期待:

- 非動画ページエラーではなく進捗を表示。
- 正常終了済みの場合は古いエラー・詳細ログを表示しない。

### TS-POP-004 二重実行防止

期待:

- 実行中は追加ボタンが無効。
- 連続クリックしてもソース追加とマインドマップ生成が1回だけ。

### TS-POP-005 ログ初期化

期待:

- 新規実行開始時に前回ログが消える。
- 拡張機能再読み込み後に前回ログが残らない。

## 8. タブ制御・画面遷移テスト

### TS-TAB-001 最新版スクリプト

- 重要度: High
- 対応要件: REQ-TAB-003

最新版が応答するタブで処理を開始し、`chrome.tabs.reload` が呼ばれないこと。

### TS-TAB-002 古いスクリプト

- 重要度: High
- 対応要件: REQ-TAB-004〜005

古いバージョンを返すタブで、1回だけ再読み込みし、30秒以内に最新版を確認すること。

### TS-TAB-003 読み込み状態遷移

- 重要度: High

`loading → complete` の `chrome.tabs.onUpdated` イベントモックで、完了通知後に自動操作を開始すること。固定間隔の状態照会を行わず、完了時にリスナーを解除すること。

### TS-TAB-004 読み込みタイムアウト

- 重要度: High

30秒間 `loading` のままの場合、明示的な日本語エラーで停止すること。

### TS-TAB-005 別ノートブックからホームへ遷移

- 重要度: Critical
- 対応要件: REQ-TAB-006〜008

最初のメッセージ通信が `message channel closed` で終了するモックを使用する。

期待:

- ページ読み込みを待つ。
- Content Scriptを再確認する。
- 同じ動画要求を再送する。
- 2回目が成功すれば全体を成功とする。

### TS-TAB-006 通常エラー

一般的なDOM操作エラーを `message channel closed` と誤認せず、再送しないこと。

### TS-TAB-007 再接続上限

5回連続で画面遷移通信切断が発生した場合、6回目を実行せず停止すること。

## 9. ノートブック選択テスト

### TS-NB-001 対象ノートブックが開いている

期待:

- ホームへ戻らず、そのノートブックを使用する。
- 同名ノートブックを追加作成しない。

### TS-NB-002 別ノートブックが開いている

- 重要度: Critical

期待:

- ホームへ戻る。
- Message Channel再接続後に処理を継続する。
- 対象チャンネルの既存ノートブックを開く。

### TS-NB-003 既存ノートブック

期待:

- 完全一致または許容される正規化一致のカードを開く。
- 実際に開いたノートブック名をログへ記録。
- 重複作成しない。

### TS-NB-004 新規ノートブック

期待:

- 1件だけ新規作成。
- チャンネル名をタイトルへ設定。
- ノートブック画面を確認してからソース処理へ進む。

### TS-NB-005 一致候補が曖昧

対象を一意に決められない場合、無関係なノートブックを開かず停止すること。

### TS-NB-006 作成ボタンだけが先に表示される

- 重要度: Critical
- 対応要件: REQ-NB-008

初期状態:

- 「ノートブックを作成」ボタンは表示済み。
- 既存ノートブックカード一覧はまだ読み込み中。

期待:

- 一覧読み込み完了と判定しない。
- 新規作成ボタンを押さない。
- カード表示後に既存ノートブックを検索する。

### TS-NB-007 作成途中の無題ノートブック

- 重要度: Critical
- 対応要件: REQ-NB-009〜010

手順:

1. 新規作成クリック前に対象チャンネル名を保存する。
2. ページ遷移でMessage Channelを切断する。
3. 遷移先を「無題のノートブック」として処理再開する。

期待:

- 無題ノートブックを作成途中として引き継ぐ。
- チャンネル名を設定する。
- ホームへ戻らない。
- 追加の新規ノートブックを作成しない。
- 命名後に一時状態を消去する。

## 10. ソース追加・重複防止テスト

### TS-SRC-001 新規ソース追加

- 重要度: Critical

期待:

- URL入力欄へCanonical URLを入力。
- 検索欄へ入力しない。
- 確定後にURL入力画面が閉じる。
- ソース数が1件だけ増える。

### TS-SRC-002 既存ソース

- 重要度: Critical

期待:

- 同じ動画IDまたは動画タイトルを検出。
- 追加ダイアログを開かない。
- ソース数が増えない。
- 既存の該当ソースを選択する。

### TS-SRC-003 同じ動画を連続実行

2回目の実行後も同じ動画ソースが1件だけであること。

### TS-SRC-004 Angular入力

入力イベントを受けないフィクスチャでは確定ボタンが有効にならず、正しいイベント列を発火した場合だけ有効になること。

### TS-SRC-005 重なったダイアログ

最前面のURL追加ダイアログだけを操作し、完了後にソースダイアログが残らないこと。

### TS-SRC-006 空状態誤判定

「保存したソースはここに表示」だけのパネルを追加済みと判定しないこと。

### TS-SRC-007 処理失敗

ソース追加を確認できない場合、マインドマップ作成へ進まないこと。

### TS-SRC-008 新規ノートブック直後のツールチップ

- 重要度: Critical
- 対応要件: REQ-SRC-010

ノートブックが開かれていない状態から対象ノートブックを開き、「ソースに基づいてAI機能を生成」等の文言を含むツールチップが表示された状態を再現する。

期待:

- ツールチップをソース追加ダイアログと判定しない。
- 本物の「ソースを追加」を操作する。
- YouTube URL追加、対象ソース1件の選択、マインドマップ作成へ継続する。

### TS-SRC-009 URL確定・挿入の二重送信防止

- 重要度: Critical
- 対応要件: REQ-SRC-011

期待:

- URL入力確定のEnterは1回。
- 「挿入」ボタンのクリックは1回。
- 合成clickイベントと `element.click()` を同じ操作へ重ねない。

### TS-SRC-010 仮想スクロールされた既存ソース

- 重要度: Critical
- 対応要件: REQ-SRC-012

対象ソースが現在描画中の行にはなく、仮想スクロール後に表示されるフィクスチャを使用する。

期待:

- 一覧全体を探索して既存ソースを発見する。
- 追加ダイアログを開かない。
- 既存ソースの選択へ進む。

## 11. ソース選択テスト

### TS-SEL-001 全選択から対象1件

- 重要度: Critical
- 対応要件: REQ-SEL-001〜010

初期状態: 全ソース選択済み。

期待クリック順:

1. 「すべて選択」
2. 対象ソース

最終状態: `[false, ..., true(target), ..., false]`

### TS-SEL-002 対象1件だけ選択済み

期待:

- クリック0回。
- 対象チェックを外さない。

### TS-SEL-003 部分選択

期待:

- 必要に応じて「すべて選択」で全選択へ揃える。
- 次の「すべて選択」で一括解除。
- 対象1件だけを選択。
- 個別ソースを1件ずつ解除しない。

### TS-SEL-004 新規ソース再描画

- 重要度: Critical

対象を選択した直後にAngular再描画でチェックが外れるフィクスチャを使用する。

期待:

- 対象を再取得して再選択。
- 最大3回。
- 最終的に対象1件だけ。

### TS-SEL-005 正式ソース行の状態待機

URLだけを表示する一時行から、動画タイトル・有効チェック・操作メニューを持つ正式行へ変わるフィクスチャを使用する。

期待:

- URLだけの一時行を選択しない。
- 処理中または無効な行を選択しない。
- 正式行の完了状態が揃えば、追加の固定時間を待たずに進む。

### TS-SEL-006 対象を特定できない

一致候補が0件のとき、すべてのソースを使用せず停止すること。

### TS-SEL-007 「すべて選択」がない

複数選択状態で一括解除UIがない場合、個別解除へフォールバックせず停止すること。

### TS-SEL-008 多数ソース性能

20件以上のソースで、個別解除回数がソース数に比例しないこと。「すべて選択」の利用をログまたはクリック記録で確認する。

### TS-SEL-009 一括解除後に対象1件だけ残る

- 重要度: Critical
- 対応要件: REQ-SEL-011

「すべて選択」を押した結果、他の全ソースが解除され、新規追加した対象ソースだけが選択された状態になるフィクスチャを使用する。

期待:

- 一括解除失敗と判定しない。
- 対象ソースのチェックを外さない。
- 追加の「すべて選択」や対象チェックをクリックしない。
- 選択数1件、選択内容が対象動画であることを確認してマインドマップ作成へ進む。

### TS-SEL-010 一括解除の画面反映が遅れる

- 重要度: Critical
- 対応要件: REQ-SEL-012

全ソース選択状態で「すべて選択」をクリックし、クリック後の複数回の状態確認ではクリック前と同じ全選択状態を返した後、全解除状態へ変わるフィクスチャを使用する。

期待:

- クリック前の古い全選択状態を操作完了と判定しない。
- 全解除状態が実際に反映されるまで状態確認を継続する。
- 対象ソースを1回だけ選択し、選択数1件・選択内容が対象動画であることを確認してマインドマップ作成へ進む。

### TS-SEL-011 同じ動画が複数登録済み

- 重要度: Critical
- 対応要件: REQ-SEL-002

同じ動画タイトルの正式ソース行が複数あるフィクスチャを使用する。

期待:

- 一致するソースのうち1件だけを選択する。
- 他の重複ソースと対象外ソースは選択しない。
- マインドマップ作成へ進む。

### TS-SEL-012 aria-labelのないチェックボックス

- 重要度: High
- 対応要件: REQ-SEL-013

チェックボックスに `aria-label` がなく、行タイトルと操作メニューは存在する現行画面相当のフィクスチャを使用する。

期待:

- 正式ソース行として認識する。
- 対象1件だけの選択へ進む。

## 12. 日本語指定テスト

### TS-LANG-001 言語コードja

英字タイトルでも `languageCode=ja` なら日本語指示文を返すこと。

### TS-LANG-002 日本語タイトル

言語コードが空でも、ひらがな・カタカナを含むタイトルなら日本語指示文を返すこと。

### TS-LANG-003 英語動画

`languageCode=en` かつ英語タイトルでは日本語指示文を入力しないこと。

### TS-LANG-004 実画面入力

- 重要度: High

日本語動画の「希望するトピック」欄へ、次が完全一致で入力されてから生成されること。

`動画の内容を日本語で整理し、マインドマップ全体を日本語で作成してください。`

生成物の主要ノードが日本語であることも確認する。

## 13. マインドマップ作成・完成判定テスト

### TS-MM-001 作成前一覧

- 重要度: Critical

作成ボタン押下前に、既存マインドマップのUUIDとタイトルを記録すること。

### TS-MM-002 UUID差分

作成後一覧へ新しいUUIDが1件増えた場合、その項目だけを新規候補にすること。

### TS-MM-003 生成中カード

次のカードを完成扱いしないこと。

- 「生成しています」
- shimmerあり
- disabledボタン
- `sync` または回転アイコン
- メニューなし

### TS-MM-004 同じUUIDの完成カード

生成中と完成でUUIDが同じ場合、完成条件が揃った後だけ候補にすること。

### TS-MM-005 完成状態判定

生成中カードでは改名処理を開始せず、完成条件がすべて揃った時点で追加の固定時間を待たずに改名処理へ進むこと。

### TS-MM-006 候補複数

作成後に未知UUIDが複数現れた場合、既存項目を推測で選ばず停止すること。

### TS-MM-007 完成タイムアウト

240秒以内に完成条件が揃わない場合、改名せずエラーにすること。

### TS-MM-008 現行画面の完成カード

- 重要度: Critical
- 対応要件: REQ-MM-006、REQ-MM-009

新規UUID、空でないタイトル、有効な操作メニューを持ち、生成中表示がない一方、`flowchart` アイコン文字列と特定のメインボタンを持たないフィクスチャを使用する。

期待:

- 完成カードとして認識する。
- 一覧総件数の厳密な `作成前+1` を要求しない。
- 新規UUIDのカードだけを改名へ渡す。

## 14. マインドマップ改名・保護テスト

### TS-REN-001 新規UUIDだけを改名

- 重要度: Critical

作成前3件、作成後4件のUUID一覧を使用する。

期待:

- 作成前にないUUID1件だけを操作。
- 既存3件のタイトルは完全に不変。

### TS-REN-002 完成前は改名しない

生成中カードが存在する間、既存カードのメニューを開かないこと。

### TS-REN-003 既存同名カード

既存カードが動画タイトルと同名でも、新規UUIDカードの改名完了とは判定しないこと。

### TS-REN-004 子要素から親カードを取得

完成候補として `.artifact-primary-content` 等の子要素が渡されても、親 `artifact-library-item` のUUIDを取得すること。

### TS-REN-005 DOM要素再利用

- 重要度: Critical

同一DOMオブジェクトのUUIDが作成前IDから新規IDへ変化するフィクスチャを使用する。

期待:

- DOM同一性を理由に既存扱いしない。
- 新規UUIDなら改名対象。

### TS-REN-006 SPA再描画

メニューを開く前、入力欄表示後、保存後にカードDOMを交換する。

期待:

- 各段階でUUIDからカードを再取得。
- 古い参照を操作しない。

### TS-REN-007 ソース名保護

ソース項目にも同じ動画タイトルとメニューボタンがある状態で、ソースメニューを開かずマインドマップカードのメニューだけを開くこと。

### TS-REN-008 最終タイトル確認

同じ新規UUIDの `.artifact-title` が動画タイトルと完全一致した場合だけ成功とすること。

## 15. デバッグ・ログテスト

### TS-LOG-001 ファイル名

期待:

- `youtube-to-gemini-notebook-debug-<日時>.json`
- `notebooklm` を含まない
- `saveAs=true`

### TS-LOG-002 JSON項目

対象動画情報、言語コード、進捗、詳細ログが存在すること。

### TS-LOG-003 重要イベント

次が追跡できること。

- ノートブック画面遷移
- 既存ソースのスキップ
- 「すべて選択」と対象チェック
- 作成前マインドマップ一覧
- 新規UUID
- 完成判定
- 改名安全判定
- 改名したUUIDと最終タイトル

### TS-LOG-004 正常終了後

正常終了した次回利用時に古いログやエラーをポップアップへ表示しないこと。

### TS-TIME-001 固定時間待機の禁止

- 重要度: High
- 対応要件: REQ-TIME-001〜005、REQ-DOM-006

期待:

- 操作コードに `await wait(...)` 等の固定sleepがない。
- `stableSince`、最低待機秒数、安定秒数を正常完了条件に使用しない。
- `setInterval` または再帰的な `setTimeout` による固定間隔ポーリングがない。
- DOM状態の再判定に `MutationObserver` と状態変更イベントを使用する。
- タブ読み込み完了の再判定に `chrome.tabs.onUpdated` を使用する。
- DOMまたはタブ状態が成立した通知と同時に完了する。
- タイムアウトは異常終了の上限としてのみ機能する。

### TS-TIME-002 イベント待機の後始末

- 重要度: High
- 対応要件: REQ-TIME-005

期待:

- DOM条件成立時に `MutationObserver`、イベントリスナー、上限タイマーを解除する。
- タブ読み込み完了時に `chrome.tabs.onUpdated` リスナーと上限タイマーを解除する。
- メッセージ応答が返った時点で応答上限タイマーを解除する。
- 正常完了後に遅延タイマーが発火しない。

## 16. 必須E2Eシナリオ

### E2E-001 新規ノートブック・新規ソース

1. 対象チャンネルのノートブックがない状態から開始。
2. 日本語字幕あり動画を追加。

期待:

- ノートブック1件を新規作成。
- 無題ノートブックを残さない。
- チャンネル名を設定。
- ソース1件を追加。
- 対象1件だけ選択。
- 日本語マインドマップを作成。
- 動画タイトルへ改名。
- 各状態が成立したら固定時間を待たず直ちに次へ進む。

### E2E-002 既存ノートブック・新規ソース・既存マップ複数

- 重要度: Critical

1. DATA-MANYとDATA-MAPSを用意。
2. 処理前の全UUIDとタイトルを保存。
3. DATA-JA-NEWを実行。

期待:

- ソース数が1だけ増える。
- 対象ソースだけ選択。
- 新規UUIDが1だけ増える。
- 新規マップ名が動画タイトル。
- 全既存マップ名が処理前と一致。

### E2E-003 既存ソース

期待:

- ソース数不変。
- 既存対象ソースだけ選択。
- 新規マップは1件作成・改名。

### E2E-004 別ノートブックから開始

期待:

- ホーム遷移後も止まらない。
- 対象ノートブックを開いた後も止まらない。
- 既存ノートブックと同名の新規ノートブックを作成しない。
- 最終処理まで完了。

### E2E-005 既存同名マップ

既存マップ1件を動画タイトルと同名にしてから実行する。

期待:

- 既存同名マップを成功判定に使わない。
- 新規UUIDのマップも同じ動画タイトルへ改名。
- 他の既存タイトルは不変。

### E2E-006 英語動画

期待:

- 日本語指示を入力しない。
- その他の追加・選択・生成・改名は成功。

### E2E-007 字幕なし

期待:

- Gemini Notebook側に変更なし。
- 明確なエラー表示。

### E2E-008 正常終了後の再利用

1. 正常終了。
2. ポップアップを閉じる。
3. 再度拡張機能を開く。

期待:

- 前回のエラーや途中ログが上部に残らない。
- 次の動画処理を開始できる。

## 17. 実画面での証跡

各Critical E2Eで次を保存すること。

- 対象YouTube URLと動画タイトル
- 処理前後のソース件数
- 処理後に選択されたソース名と選択数
- 作成前後のマインドマップUUID一覧
- 作成前後のマインドマップタイトル一覧
- 日本語指示欄の値
- 最終マインドマップタイトル
- デバッグJSON

スクリーンショットだけでUUIDを確認できない場合は、読み取り専用DOM検査または詳細ログを証跡とする。

## 18. テスト時の安全規則

- 既存マインドマップのタイトルをテスト準備で変更する場合は専用テストノートブックだけを使う。
- 本番用ノートブックで削除テストを行わない。
- 失敗時に対象外ソースや既存マインドマップを操作して続行しない。
- E2E後は、追加したテスト用ソース・マインドマップを記録し、ユーザーの許可なく削除しない。

## 19. リリース判定チェックリスト

- [ ] 自動テストが全件成功
- [ ] JavaScript構文検査が成功
- [ ] 作業フォルダーと配布フォルダーの対象ファイルが一致
- [ ] E2E-001〜008が成功
- [ ] 既存ソース名の変更が0件
- [ ] 既存マインドマップ名の変更が0件
- [ ] 新規マインドマップ名が動画タイトルと一致
- [ ] 日本語動画の生成内容が日本語
- [ ] デバッグファイル名と内容が仕様どおり
- [ ] 正常終了後の古いログが非表示
- [ ] 固定sleep、固定間隔ポーリング、安定待機が0件
- [ ] 状態監視と上限タイマーが正常完了時に解除される
- [ ] 要求仕様・テスト仕様・再実装資料が現在の実装と一致

## 20. 追加修正のトレーサビリティ

| 修正対象 | 要求仕様 | 自動・仕様テスト |
|---|---|---|
| Gemini Notebookへの名称・URL移行 | REQ-ID-001〜003 | TS-STATIC-001〜002 |
| 動画ページ誤判定とパネル重複表示 | REQ-POP-001〜003 | TS-POP-001〜003 |
| 画面遷移後の処理再開 | REQ-TAB-006〜008 | TS-TAB-005〜007、E2E-004 |
| 一覧読込前の重複ノートブック作成 | REQ-NB-008 | TS-NB-006 |
| 無題・同名ノートブックの重複作成 | REQ-NB-009〜010 | TS-NB-007、E2E-001 |
| ツールチップのソース画面誤認 | REQ-SRC-010 | TS-SRC-008 |
| 同一動画ソースの重複追加 | REQ-SRC-001〜003 | TS-SRC-002〜003 |
| YouTubeのタイトル・サムネイルA/Bテスト | REQ-YT-007〜008 | TS-YT-003〜004 |
| URL確定・挿入の二重送信 | REQ-SRC-011 | TS-SRC-009 |
| 仮想スクロールされた既存ソース | REQ-SRC-012 | TS-SRC-010 |
| 「すべて選択」による高速な一括解除 | REQ-SEL-005〜006 | TS-SEL-001、003、008 |
| 新規ソース選択の再描画対応 | REQ-SEL-003、007〜010 | TS-SEL-004〜006 |
| 一括解除後に対象1件だけ残る状態 | REQ-SEL-011 | TS-SEL-009 |
| クリック直後の古い選択状態 | REQ-SEL-012 | TS-SEL-010 |
| 重複済みソースとaria-labelなしチェック | REQ-SEL-002、013 | TS-SEL-011〜012 |
| 日本語動画の日本語生成 | REQ-LANG-001〜005 | TS-LANG-001〜004 |
| 新規マップ完成後だけ改名 | REQ-MM-002〜008、REQ-REN-001〜009 | TS-MM-001〜007、TS-REN-001〜008 |
| 現行画面のマップ完成判定 | REQ-MM-006、009 | TS-MM-008 |
| 既存マップ・ソース名の保護 | REQ-REN-002、008〜009 | TS-REN-001〜003、007〜008 |
| 正常終了後のログ初期化 | REQ-POP-006〜007、REQ-LOG-006 | TS-POP-005、TS-LOG-004 |
| 固定時間待機と固定間隔ポーリングの廃止 | REQ-TIME-001〜005、REQ-DOM-006 | TS-TIME-001〜002、TS-TAB-003 |
