# YouTube to Gemini Notebook Mind Map 再実装資料

## 文書の位置付け

このファイルは、再実装時に参照すべき正本を示す索引である。

- 要求仕様の正本: `REQUIREMENTS_SPECIFICATION.md`
- テスト仕様の正本: `TEST_SPECIFICATION.md`
- 同梱自動テストの説明: `TESTING.md`

本ファイルへ別系統の要求を重複記載しない。機能変更、不具合修正、待機条件の変更は、必ず要求仕様とテスト仕様の両方へ同じ識別番号の対応関係が分かる形で反映する。

## 現在の製品識別情報

| 項目 | 値 |
|---|---|
| 拡張機能名 | `YouTube to Gemini Notebook Mind Map` |
| 配布フォルダー | `YouTubeToGeminiNotebookMindMap` |
| Gemini Notebook URL | `https://notebook.google.com/` |
| デバッグファイル | `youtube-to-gemini-notebook-debug-<ISO日時>.json` |
| 現在のManifestバージョン | `0.2.7` |

## 再実装時に省略してはならない要点

1. YouTube動画タイトル、oEmbedのソース照合用タイトル、チャンネル名、字幕と言語情報を取得し、SPA遷移時は動画IDの一致を確認する。
2. 既存ノートブックを優先し、同名・無題ノートブックを重複作成しない。
3. 同一動画ソースを重複追加しない。
4. 「すべて選択」で一括解除し、対象ソース1件だけを選択する。
5. 日本語動画には日本語マインドマップ作成指示を設定する。
6. 作成前後のUUID一覧差分で新規マインドマップだけを特定する。
7. DOM上の完成条件をすべて確認した後、新規UUIDのマインドマップだけを動画タイトルへ改名する。
8. 既存マインドマップ名とソース名を変更しない。
9. ツールチップをソース追加ダイアログと誤認しない。
10. 正常処理では固定sleep、固定間隔ポーリング、安定待機を使用しない。
11. 仮想スクロールされたソース一覧全体を探索し、同じ動画が複数登録済みでも1件だけを選択する。
12. URL確定Enterとボタンクリックを二重送信しない。
13. マインドマップ完成判定では新規UUID、非生成中、タイトル、操作メニューを使用し、`flowchart` アイコン文字列や特定メインボタンを必須にしない。

## 状態待ちの原則

- DOM状態は `MutationObserver` と入力・変更・クリック・画面遷移等の状態変更イベントで再判定する。
- タブ読み込み状態は `chrome.tabs.onUpdated` で再判定する。
- 条件成立時は追加時間を待たず直ちに次へ進む。
- 時間指定は異常終了の上限タイムアウトにだけ使用する。
- 完了時は監視リスナーと上限タイマーを解除する。

詳細な条件、上限、禁止事項、全テストケースは正本2文書を参照すること。
